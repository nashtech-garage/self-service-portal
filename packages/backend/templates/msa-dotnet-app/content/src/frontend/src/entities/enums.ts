export enum AssignmentStatus {
  PENDING = 1,
  ACCEPTED = 2,
  DECLINED = 3,
  RETURNED = 4,
}

export enum UserTypeEnum {
  ADMIN = 1,
  STAFF = 2,
}

export enum SortDirection {
  ASC = 1,
  DESC = -1,
}

export enum GenderEnum {
  FEMALE = 0,
  MALE = 1,
}
